package nodes.instructions.conditionals;

import nodes.instructions.InstrNode;

public class CondNode extends InstrNode {
    public CondNode(int line, int column) {
        super(line, column);
    }
}
